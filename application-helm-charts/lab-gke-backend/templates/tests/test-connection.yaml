apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab-gke-backend.fullname" . }}-test-connection"
  labels:
    {{- include "lab-gke-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab-gke-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
