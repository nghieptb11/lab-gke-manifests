apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab-gke-backend.fullname" . }}
  labels:
    {{- include "lab-gke-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab-gke-backend.selectorLabels" . | nindent 4 }}
